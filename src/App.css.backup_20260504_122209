/* App.css - Global styles for the digital book app - FIXED RESPONSIVE */

/* FORCE FULL VIEWPORT - Override everything */
html, body, #root {
  margin: 0 !important;
  padding: 0 !important;
  width: 100vw !important;
  height: 100vh !important;
  overflow: hidden !important;
  box-sizing: border-box !important;
}

* {
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* MAIN APP CONTAINER - FORCE FULL VIEWPORT */
.App {
  width: 100vw !important;
  height: 100vh !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  overflow: hidden !important;
  margin: 0 !important;
  padding: 0 !important;
}

/* Ensure images are responsive */
img {
  max-width: 100%;
  height: auto;
}

/* Touch-friendly buttons */
button {
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
}

/* Prevent text selection on interactive elements */
.interactive-element {
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  user-select: none;
}

/* Mobile-first responsive design */
@media (max-width: 768px) {
  body {
    font-size: 14px;
  }
  
  button {
    min-height: 44px; /* iOS recommended touch target size */
    min-width: 44px;
  }
}

/* Smooth transitions */
.transition-smooth {
  transition: all 0.3s ease;
}

/* Loading states - FULL VIEWPORT */
.loading {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh !important;
  width: 100vw !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
}

.loading::after {
  content: "";
  width: 50px;
  height: 50px;
  border: 5px solid #f3f3f3;
  border-top: 5px solid #3498db;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* Loading screen for app initialization - FULL VIEWPORT */
.loading-screen {
  width: 100vw !important;
  height: 100vh !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  z-index: 9999;
}

.loading-spinner {
  color: white;
  font-size: 24px;
  font-weight: bold;
  animation: fadeInOut 1.5s ease-in-out infinite;
}

@keyframes fadeInOut {
  0%, 100% { opacity: 0.5; }
  50% { opacity: 1; }
}

/* Error screen - FULL VIEWPORT */
.error-screen {
  width: 100vw !important;
  height: 100vh !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #ff6b6b 0%, #f06292 100%);
  color: white;
  text-align: center;
  padding: 20px;
  z-index: 9999;
}

.error-screen h1 {
  font-size: min(36px, 8vw);
  margin-bottom: 20px;
}

.error-screen p {
  font-size: min(18px, 4vw);
  margin-bottom: 30px;
  max-width: 500px;
}

.error-screen button {
  padding: 15px 40px;
  background-color: white;
  color: #ff6b6b;
  border: none;
  border-radius: 30px;
  font-size: min(18px, 4vw);
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  min-height: 44px;
}

.error-screen button:hover {
  transform: scale(1.05);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

/* Scene placeholder for unimplemented scenes - FULL VIEWPORT */
.scene-placeholder {
  width: 100vw !important;
  height: 100vh !important;
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  text-align: center;
  padding: 20px;
  z-index: 1000;
}

.scene-placeholder h2 {
  font-size: min(32px, 6vw);
  margin-bottom: 20px;
}

.scene-placeholder p {
  font-size: min(18px, 4vw);
  margin-bottom: 30px;
  opacity: 0.9;
}

.scene-placeholder button {
  padding: 15px 40px;
  background-color: white;
  color: #667eea;
  border: none;
  border-radius: 30px;
  font-size: min(18px, 4vw);
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  min-height: 44px;
}

.scene-placeholder button:hover {
  transform: scale(1.05);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

/* Error states */
.error-message {
  background-color: #ffebee;
  color: #c62828;
  padding: 15px;
  border-radius: 5px;
  margin: 10px;
  text-align: center;
}

/* Overlay for modals - FULL VIEWPORT */
.modal-overlay {
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100vw !important;
  height: 100vh !important;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

/* Accessibility - Focus indicators */
button:focus,
a:focus,
input:focus {
  outline: 2px solid #4CAF50;
  outline-offset: 2px;
}

/* Reduced motion support */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* MOBILE RESPONSIVE OVERRIDES */
@media (max-width: 768px) {
  .error-screen h1,
  .scene-placeholder h2 {
    font-size: min(28px, 7vw);
  }
  
  .error-screen p,
  .scene-placeholder p {
    font-size: min(16px, 4vw);
  }
  
  .error-screen button,
  .scene-placeholder button {
    padding: 12px 30px;
    font-size: min(16px, 4vw);
  }
}

@media (max-width: 480px) {
  .error-screen h1,
  .scene-placeholder h2 {
    font-size: min(24px, 6vw);
  }
  
  .error-screen p,
  .scene-placeholder p {
    font-size: min(14px, 3.5vw);
  }
  
  .error-screen button,
  .scene-placeholder button {
    padding: 10px 25px;
    font-size: min(14px, 3.5vw);
  }

  /* ========================================
   RESPONSIVE UI SCALING FOR CHILDREN'S APP
   ======================================== */

/* Base mobile styles - no scaling needed */
.ui-element, button, .modal, .dialog, .popup, .overlay {
  transition: transform 0.3s ease;
}

/* Tablet scaling (768px+) */
@media screen and (min-width: 768px) {
  /* Scale up UI elements for better visibility */
  button:not(.game-element):not(.animal):not(.item):not(.decoration),
  .modal,
  .dialog,
  .popup,
  .status-message,
  .progress-bar,
  .control-panel,
  .scene-modal,
  .test-reload-button,
  .skip-button {
    transform: scale(1.3);
    transform-origin: center;
  }
  
  /* Scale text in modals and dialogs */
  .modal h1, .modal h2, .modal h3,
  .dialog h1, .dialog h2, .dialog h3,
  .popup h1, .popup h2, .popup h3 {
    font-size: 1.3em;
  }
  
  .modal p, .dialog p, .popup p {
    font-size: 1.2em;
  }
}

/* Desktop scaling (1024px+) */
@media screen and (min-width: 1024px) {
  button:not(.game-element):not(.animal):not(.item):not(.decoration),
  .modal,
  .dialog,
  .popup,
  .status-message,
  .progress-bar,
  .control-panel,
  .scene-modal,
  .test-reload-button,
  .skip-button {
    transform: scale(1.6);
    transform-origin: center;
  }
  
  .modal h1, .modal h2, .modal h3,
  .dialog h1, .dialog h2, .dialog h3,
  .popup h1, .popup h2, .popup h3 {
    font-size: 1.6em;
  }
  
  .modal p, .dialog p, .popup p {
    font-size: 1.4em;
  }
}

/* Large screen scaling (1400px+) */
@media screen and (min-width: 1400px) {
  button:not(.game-element):not(.animal):not(.item):not(.decoration),
  .modal,
  .dialog,
  .popup,
  .status-message,
  .progress-bar,
  .control-panel,
  .scene-modal,
  .test-reload-button,
  .skip-button {
    transform: scale(1.8);
    transform-origin: center;
  }
  
  .modal h1, .modal h2, .modal h3,
  .dialog h1, .dialog h2, .dialog h3,
  .popup h1, .popup h2, .popup h3 {
    font-size: 1.8em;
  }
  
  .modal p, .dialog p, .popup p {
    font-size: 1.6em;
  }
}

/* Prevent game elements from being affected */
.game-element,
.animal,
.item,
.decoration,
.kurume-deva-game .animal,
.kurume-deva-game .item,
.kurume-deva-game .decoration {
  transform: none !important;
}

/* Ensure proper spacing for scaled elements */
@media screen and (min-width: 768px) {
  .modal, .dialog, .popup {
    margin: 20px auto;
  }
}

@media screen and (min-width: 1024px) {
  .modal, .dialog, .popup {
    margin: 40px auto;
  }
}
}